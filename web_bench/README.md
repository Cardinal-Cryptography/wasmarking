This is a benchmark generating a proof for withdraw relation with WebAssembly threads.

Install dependencies:
`yarn install`

Build webpage:
`yarn build`


Install serve:
`npm install --global serve`

Serve it:
`serve dist`
